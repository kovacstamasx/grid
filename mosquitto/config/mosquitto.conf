# Enable persistence
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest file /mosquitto/log/mosquitto.log

# Allow anonymous access (for testing only, not recommended for production)
allow_anonymous true

# Listen on default MQTT port
listener 1883

# Enable WebSocket support (optional)
listener 9001
protocol websockets
